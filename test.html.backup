<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Page</title>
        <style>
  :root {
    --radius: 10vw;
    --invis-radius: 50vw;
    --invis-offset: -50vw;
    --offset: 15vw;
    --c-bg: #F4F3F0;
    --c-accent: #FF5E2B;
    --c-text: #222;
    --item-opacity-default: 0.2;
    --snap-height: 40vh;
  }

  * { box-sizing: border-box; }

  body, html {
    height: 100vh;
    margin: 0;
    padding: 0;
    background: var(--c-bg);
    overflow: hidden;
    min-width: 320px;
    font-family: 'Libre Caslon Text', 'Times New Roman', serif;
  }


  .container {
    display: flex;
    flex-direction: row;
    justify-content: space-between;
    align-items: center;
    height: 100vh;
    width: 100vw;
    gap: 2rem;
    padding: 0 2rem;
  }

  .left {
    flex: 0 0 35%;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  @media (min-width: 768px) {
    .left {
      padding: 50px;
    }
  }

  .left h1 {
    font-size: clamp(1.5rem, 3vw, 2.5rem);
    line-height: 1.3;
    margin: 0;
  }

  .right {
    flex: 1;
    height: 100vh;
    position: relative;
  }

  .scroller {
    scroll-snap-type: y mandatory;
    height: 100vh;
    overflow-y: scroll;
    position: relative;
    z-index: 1;
    -webkit-overflow-scrolling: touch;
  }

  .buffer { height: 100vh; scroll-snap-align: start; }
  .snap { height: var(--snap-height); scroll-snap-align: end; }

  .spinner-wrap {
    width: 100vw;
    height: 100vh;
    overflow: hidden;
    position: absolute;
    top: 0;
    left: 0;
  }

  .spinner {
    position: absolute;
    top: -9999px;
    bottom: -9999px;
    left: var(--invis-offset);
    width: calc(var(--radius) * 2);
    height: calc(var(--radius) * 2);
    margin: auto;
    transform-origin: 50% 50%;
    will-change: transform;
  }

  .dot {
    position: absolute;
    top: 0;
    left: calc(50vw - var(--invis-radius) + 3.5vw);
    bottom: 0;
    margin: auto;
    width: 16vw;
    height: 16vw;
    object-fit: contain;
    will-change: transform;
  }

  .item-wrap {
    position: absolute;
    left: 50%;
    top: 0;
    bottom: 0;
    margin: auto;
  }

  .item {
    position: absolute;
    left: 0;
    top: 0;
    bottom: 0;
    width: 40vw;
    display: flex;
    align-items: center;
    font-size: clamp(14px, 2.5vw, 24px);
    white-space: normal;
    letter-spacing: -0.02em;
    font-family: 'Libre Caslon Text', 'Times New Roman', serif;
    transform-origin: 0 50%;
    opacity: var(--item-opacity-default);
    color: var(--c-text);
    transition: opacity 150ms ease;
    will-change: transform, opacity;
    transform: rotate(var(--deg)) translateX(calc(var(--invis-radius) + 12vw));
    line-height: 1.2;
    text-decoration: none;
    cursor: pointer;
  }

  .item:hover {
    opacity: 1;
  }

  .item.active { opacity: 1; }

  /* Position each item around the circle */
  .item:nth-child(1) { --deg: -28deg; }
  .item:nth-child(2) { --deg: -30deg; }
  .item:nth-child(3) { --deg: -22deg; }
  .item:nth-child(4) { --deg: -14deg; }
  .item:nth-child(5) { --deg: -16deg; }
  .item:nth-child(6) { --deg: -8deg; }
  .item:nth-child(7) { --deg: 0deg; }
  .item:nth-child(8) { --deg: 8deg; }
  .item:nth-child(9) { --deg: 16deg; }
  .item:nth-child(10) { --deg: 24deg; }
  .item:nth-child(11) { --deg: 32deg; }
  .item:nth-child(12) { --deg: 40deg; }
  .item:nth-child(13) { --deg: 48deg; }

  @media (prefers-reduced-motion: reduce) {
    .spinner, .dot, .item { transition: none; }
  }

  @font-face {
    font-family: 'LibreCaslonCondensed';
    src: url('https://assets.codepen.io/215059/LibreCaslonCondensed-Regular.otf') format('opentype');
    font-weight: normal;
    font-style: normal;
  }

  .explainer {
    position: absolute;
    right: 16px;
    top: 16px;
    background-color: #fff;
    width: 300px;
    padding: 24px;
    padding-right: 40px;
    display: none;
    z-index: 2;
    font-family: ui-monospace, SFMono-Regular, 'SF Mono', Menlo, Consolas, 'Liberation Mono', monospace;
  }

  .explainer input, .explainer div {
    position: absolute;
    top: 8px;
    right: 8px;
    appearance: none;
    width: 30px;
    height: 30px;
  }

  .explainer div {
    display: flex;
    align-items: center;
    justify-content: center;
    pointer-events: none;
    background-color: rgba(0, 0, 0, 0.05);
    border-radius: 50%;
  }

  .explainer div::before { content: '×'; }

  .explainer:has(input:checked) div::before { content: 'i'; }

  .explainer:has(input:checked) p { display: none; }

  .explainer:has(input:checked) { background-color: transparent; }

  .explainer a {
    color: inherit;
    font-family: inherit;
  }

  .explainer p {
    font-size: 12px;
    line-height: 18px;
  }

  .explainer p ~ p { margin-top: 1em; }

  @media (min-width: 320px) {
    .explainer { display: block; }
  }
        </style>
</head>
<body>
    <div class="container">
    <div class="left"> <h1> These prominent news soirces are talking about us and our products </h1></div>
    
    
    <div class="right">
    <div class="scroller">
      <div class="buffer"></div>
      <div class="snap"></div>
      <div class="snap"></div>
      <div class="snap"></div>
      <div class="snap"></div>
      <div class="snap"></div>
      <div class="snap"></div>
      <div class="snap"></div>
      <div class="snap"></div>

      <div class="spinner-wrap">
        <div class="spinner">
          <div class="item-wrap">
            <div class="item">“FileScience: Nick Martin’s mission to secure the cloud’s overlooked data” </div>
            <div class="item">“FileScience: Nick Martin’s mission to secure the cloud’s overlooked data” </div>
            <div class="item">“FileScience: Nick Martin’s mission to secure the cloud’s overlooked data” </div>
            <div class="item">Make a song for my friend Earl.</div>
            <div class="item">“FileScience: Nick Martin’s mission to secure the cloud’s overlooked data” </div>
            <div class="item">Make a song about mum's cooking.</div>
            <div class="item">Make a song for lunch.</div>
            <div class="item">Make a song for a road trip.</div>
            <div class="item">Make a song for a workout.</div>
            <div class="item">Make a song for a breakup.</div>
            <div class="item">Make a happy song.</div>
            <div class="item">Make a song about mitochrondria.</div>
            <div class="item">Make a song about how you feel.</div>
          </div>
        </div>
        <img class="dot" src="" alt="Visual indicator">
      </div>
    </div>

    <div class="explainer">
      <input type="checkbox" checked>
      <div></div>
      <p>Scroll to rotate the ring; JS drives the transform for Safari.</p>
      <p>Works in modern browsers.</p>
    </div>

    <script>
      (function() {
        const scroller = document.querySelector('.scroller');
        const spinner = document.querySelector('.spinner');
        const spinnerWrap = document.querySelector('.spinner-wrap');
        const dot = document.querySelector('.dot');
        const items = Array.from(document.querySelectorAll('.item'));
        if (!scroller || !spinner || !dot || !items.length) return;

        const offsetVW = parseFloat(getComputedStyle(document.documentElement).getPropertyValue('--offset')) || 15;
        const maxRotate = -96; // degrees, roughly matching original range
        let currentProgress = 0;
        
        // Define images for each item - cycling through the 3 available images
        const images = [
          '..//images/1.svg',
          '..//images/2.svg',
          '..//images/3.svg',
          '..//images/1.svg',
          '..//images/2.svg',
          '..//images/3.svg',
          '..//images/1.svg',
          '..//images/2.svg',
          '..//images/3.svg',

        ];

        const update = (progress) => {
          if (progress !== undefined) {
            currentProgress = Math.max(0, Math.min(1, progress));
          } else {
            const maxScroll = scroller.scrollHeight - scroller.clientHeight;
            currentProgress = maxScroll > 0 ? scroller.scrollTop / maxScroll : 0;
          }

          spinner.style.transform = `rotate(${maxRotate * currentProgress}deg)`;

          const activeIndex = Math.min(items.length - 1, Math.round(currentProgress * (items.length - 1)));
          items.forEach((el, idx) => el.classList.toggle('active', idx === activeIndex));
          
          // Update dot image based on active item
          if (images[activeIndex]) {
            dot.src = images[activeIndex];
          }
        };

        // Prevent scroll on spinner-wrap hover and use wheel delta instead
        spinnerWrap.addEventListener('wheel', (e) => {
          e.preventDefault();
          e.stopPropagation();
          
          // Adjust progress based on wheel delta
          const delta = e.deltaY / 1000; // Normalize the delta
          update(currentProgress + delta);
        }, { passive: false });

        scroller.addEventListener('scroll', () => update(), { passive: true });
        window.addEventListener('resize', () => update(), { passive: true });
        update();
      })();

    </script>
    </div>
    </div>
</body>
</html>